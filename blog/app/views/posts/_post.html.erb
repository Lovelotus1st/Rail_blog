<%= div_for @post do %>
	<h2><%= link_to_unless_current h(@post.title) %></h2>
	<h4><%= post.created_at.strftime("%B %d, %Y") %></h4>
	<span style="color: rgb(255,0, 0);">
	<h4>Author: <%= link_to_unless_current h(@post.author) %></h5>
	</span>
	<%= simple_format h(@post.body) %>
<% end %>